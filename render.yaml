services:
  - type: web
    name: pipeline-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        value: postgresql+asyncpg://postgres:Balili0000%2A%40@db.tiiircotimeftvllqglv.supabase.co:6543/postgres
